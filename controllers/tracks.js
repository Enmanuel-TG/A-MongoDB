const {tracksModel} = require("../models/index")

//Obtiene datos a trabjar de la base de datos.
const getItems = (req, res) => {
    console.log(" Hola")
 };

const getItem = (req, res) => {
        /**
         * @param {*}req
         * @param {*}res
         */
};
const createItem = (req, res) => {
  /**
   *
   * @param {*}req
   * @param {*}res
   */
};
const updataItem = (req, res) => {
  /**
   *
   * @param {*}req
   * @param {*}res
   */
};
const deleteItem = (req, res) => {
  /**
   *
   * @param {*}req
   * @param {*}res
   */
};

// module.exports = { getItems, getItem, createItem, updataItem, deleteItem };

module.exports = { getItem, getItems, createItem, updataItem, deleteItem };